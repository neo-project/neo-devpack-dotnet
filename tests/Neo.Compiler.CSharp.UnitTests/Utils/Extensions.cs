using System.IO;

namespace Neo.Compiler.CSharp.UnitTests.Utils
{
    static class Extensions
    {
        public static readonly string TestContractRoot = Path.GetFullPath("../../../../Neo.Compiler.CSharp.TestContracts/") + "/";
    }
}
